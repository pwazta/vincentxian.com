# Database
# For PostgreSQL, use a connection string like:
# postgresql://user:password@localhost:5432/dbname
# For SQLite (development), use:
# file:./db.sqlite
DATABASE_URL="postgresql://user:password@localhost:5432/vinx_dev_3d"

# Optional: Auth Secret (auto-generated in production, optional in dev)
# Generate with: openssl rand -base64 32
AUTH_SECRET="optional_in_development"
